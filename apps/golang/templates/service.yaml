apiVersion: v1
kind: Service
metadata:
  name: golang-service
spec:
  selector:
    app: golang
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodeport: 30080
  type: NodePort  # or ClusterIP, NodePort